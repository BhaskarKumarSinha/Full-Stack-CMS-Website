# ==============================================================================
# Production Environment Variables Template
# ==============================================================================
# Copy this file to .env.production and fill in your values
# NEVER commit the actual .env.production file with real credentials!
#
# Usage:
#   cp .env.production.example .env.production
#   docker-compose -f docker-compose.prod.yml --env-file .env.production up -d
# ==============================================================================

# -----------------------------------------------------------------------------
# Security Configuration
# IMPORTANT: Change these values in production!
# -----------------------------------------------------------------------------

# JWT Secret - Use a strong, random string (at least 32 characters)
# Generate one: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-me-in-production

# JWT Token Expiration (e.g., 1h, 24h, 7d)
JWT_EXPIRES_IN=24h

# -----------------------------------------------------------------------------
# URL Configuration
# -----------------------------------------------------------------------------

# Frontend URL (for CORS whitelist)
FRONTEND_URL=http://localhost

# API Base URL (used for generating absolute URLs)
API_BASE_URL=http://localhost:4000

# File Base URL (used for media asset URLs)
FILE_BASE_URL=http://localhost:4000

# Vite API Base (embedded during frontend build)
VITE_API_BASE=http://localhost:4000

# -----------------------------------------------------------------------------
# Rate Limiting
# Adjust based on your expected traffic
# -----------------------------------------------------------------------------

# Time window in milliseconds (default: 60000 = 1 minute)
RATE_LIMIT_WINDOW_MS=60000

# Maximum requests per window for general endpoints
RATE_LIMIT_MAX=200

# Maximum requests per window for sensitive endpoints (auth, admin)
RATE_LIMIT_STRICT=20

# -----------------------------------------------------------------------------
# AWS S3 Configuration (Optional)
# Leave empty to use local file storage
# -----------------------------------------------------------------------------

# AWS Region (e.g., us-east-1, eu-west-1)
AWS_REGION=

# AWS Access Key ID
AWS_ACCESS_KEY_ID=

# AWS Secret Access Key
AWS_SECRET_ACCESS_KEY=

# S3 Bucket Name
S3_BUCKET=

# -----------------------------------------------------------------------------
# MongoDB Configuration (Optional)
# Only needed if using external MongoDB (Atlas, etc.)
# Default: mongodb://mongo:27017/cms (Docker internal network)
# -----------------------------------------------------------------------------

# MongoDB Connection String
# For MongoDB Atlas: mongodb+srv://user:password@cluster.mongodb.net/cms
# MONGODB_URI=mongodb://mongo:27017/cms

# -----------------------------------------------------------------------------
# Redis Configuration (Optional)
# Only needed if using external Redis
# Default: redis://redis:6379 (Docker internal network)
# -----------------------------------------------------------------------------

# Redis Connection URL
# REDIS_URL=redis://redis:6379
